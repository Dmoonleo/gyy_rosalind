#include <iostream>
#include "_library.h"
using namespace std;

int sssl(string a, string b)
{
	if(a.length() == 0)
		return b.length();
	if(b.length() == 0)
		return a.length();
}

string sss(string a, string b)
{

}

int main()
{
	string a;
	string b;
	a = getDNA();
	b = getDNA();
}